#include <stdio.h>

/**
 * main - print alpha in lower case and upper case
 * Retrun: Always 0
 * Description: 'The programs description'
 */
int main(void)
{
	int ch;
	char ch;

	for (ch = 'a'; ch <= 'z' ch++)
		putchar(ch);

	for (ch = 'A'; ch <= 'Z'; ch++)
		putchar(ch);

	putchar('\n');
	return (0);
}
