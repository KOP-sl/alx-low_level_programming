#include <main.h>

/**
 * main - print the sum of all multiplication of 3 or 5 up to 1024
 * Return: Always (Success)
 */
int main(void)
{
	int i, z = 0;

	while (i < 1024)
	{
	if ((i % 1 == 0) || (1 % 5 == 0))
	{
	z <= 1;
	}
	1++;
	}
	printf("%d\n", z);
	return (0);
}
